🚀 Task Management (Канбан-доска)
🎯 Цель проекта
Цель проекта — разработка простого и функционального веб-приложения типа Канбан-доски для управления задачами. Приложение должно обеспечивать визуальное представление рабочего процесса, позволяя пользователю добавлять, просматривать и удалять задачи в различных статусах (DRAFT, IN PROGRESS, EDITING, DONE).

Особое внимание уделено стилизации, приближенной к макету, и использованию чистого Vanilla JavaScript для реализации бизнес-логики (хранение данных в LocalStorage и разделение на модули: Data, UI, Controller).

✨ Описание проекта
Проект представляет собой одностраничное веб-приложение (SPA), реализованное в виде единственного HTML-файла с встроенными стилями CSS и скриптами JavaScript.

Основной функционал:
Визуальная доска: Задачи распределены по четырем колонкам, соответствующим их статусу.

Тёмная тема (Dark Theme): Стильный и контрастный дизайн, приближенный к современным UI-макетам.

Добавление задач: Форма в нижней части экрана позволяет быстро создать новую задачу с названием, описанием и начальным статусом.

Сохранение данных: Все задачи сохраняются в браузере с помощью LocalStorage, что обеспечивает их сохранность после перезагрузки страницы.

Архитектура: Использование модульной структуры (Data, UI, Controller) внутри одного HTML-файла для лучшей организации кода.

📂 Структура проекта
Весь проект реализован в одном файле index.html. Код разделен на следующие логические блоки:

<head>/<style>: Стилизация, отвечающая за темную тему, компоновку Flexbox (доска и колонки) и внешний вид карточек.

<body> (HTML):

Шапка (header).

Основное содержимое (main-content), включающее:

Канбан-доску с колонками (kanban-board-container).

Правый сайдбар (имитация метрик).

Форма добавления задачи (add-task-form).

<script type="module" id="data-service"> (Модель): Ответственен за работу с данными: loadTasks(), saveTasks(), addTask(), deleteTask(), используя Map и LocalStorage.

<script type="module" id="ui-service"> (Представление): Ответственен за рендеринг: createCardElement(), renderBoard(), работу с формой.

<script type="module" id="main-controller"> (Контроллер): Связывает Модель и Представление, обрабатывает события (handleAdd, handleDelete) и вызывает refreshBoard().

<script> (Unit Test): Вспомогательный скрипт для тестирования логики данных.

### 🖼️ Интерфейс и Дизайн

Приложение имеет минималистичный, современный дизайн в тёмной цветовой гамме. Акцентные цвета используются для визуального выделения статуса каждой колонки и карточки.

 
**Пример вставки кода скриншота:**

```markdown
![Общий вид канбан-доски в темной теме]()

🛠️ Инструкция по запуску
Для запуска проекта не требуется установка сервера.

Сохранение: Скопируйте весь предоставленный код и сохраните его в файл с именем, например, index.html.

Запуск: Откройте файл index.html в любом современном веб-браузере (Chrome, Firefox, Edge).

Готово: Доска загрузится. Если LocalStorage пуст, будут автоматически загружены задачи по умолчанию.

Тестирование:
Добавление: Заполните форму внизу и нажмите "Добавить Задачу". Задача появится в соответствующей колонке.

Удаление: Нажмите кнопку "🗑 Удалить" на карточке задачи, чтобы удалить ее из системы.

Unit-тест: Нажмите кнопку "Запустить Unit-тест" в левом нижнем углу, чтобы проверить корректность работы функций сохранения/загрузки данных (результаты выводятся в консоль разработчика).

💻 Примеры кода (Ключевые фрагменты)
Асинхронная загрузка/сохранение данных (Data Service)
Ключевой момент — использование Map для эффективного хранения задач по их id и localStorage для персистентности:

JavaScript

// Data Service (Фрагмент)
const STORAGE_KEY = 'kanbanData';
let tasks = new Map(); 

function loadTasks() {
    const data = localStorage.getItem(STORAGE_KEY);
    if (data) {
        const arrayData = JSON.parse(data);
        tasks = new Map(arrayData.map(item => [item.id, item]));
    }
}

function saveTasks() {
    // Преобразование Map в массив объектов для сохранения в JSON
    const arrayData = Array.from(tasks.values());
    localStorage.setItem(STORAGE_KEY, JSON.stringify(arrayData));
}
Рендеринг карточки (UI Service)
Динамическое создание элемента задачи и привязка обработчика удаления:

JavaScript

// UI Service (Фрагмент)
function createCardElement(task, onDelete) {
    const card = document.createElement('div');
    card.className = 'task-card';
    card.dataset.id = task.id;
    
    // Динамическая установка цвета боковой полоски
    let borderColor = ''; 
    // ... (логика определения цвета по статусу) ...
    
    card.style.borderLeftColor = borderColor;

    card.innerHTML = `...`; // Содержимое карточки

    const deleteButton = card.querySelector('.delete-btn');
    deleteButton.addEventListener('click', () => onDelete(task.id));
    return card;
}
📝 Выводы по работе
Модульность без сборщиков: Проект успешно демонстрирует реализацию модульной архитектуры (разделение обязанностей между Data, UI, Controller) внутри одного HTML-файла с использованием type="module" и глобального объекта window.App, что упрощает разработку на чистом JS.

UX/UI: Тёмная тема и четкое визуальное разделение колонок с помощью Flexbox делают интерфейс интуитивно понятным и эстетически привлекательным.

Персистентность: Использование LocalStorage обеспечивает базовый уровень сохранения данных, что критически важно для любого инструмента управления задачами.

Дальнейшее развитие: Следующим шагом может стать добавление функционала Drag and Drop (DND) для перемещения задач между колонками, что является ключевой особенностью Канбан-досок.
